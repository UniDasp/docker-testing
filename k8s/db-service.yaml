apiVersion: v1
kind: Service
metadata:
  name: mi-db
  namespace: docker-testing
  labels:
    app: mi-db
spec:
  type: ClusterIP
  selector:
    app: mi-db
  ports:
    - name: http
      port: 3002
      targetPort: 3002
