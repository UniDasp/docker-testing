apiVersion: v1
kind: Service
metadata:
  name: mi-db
  namespace: docker-testing
  labels:
    app: mi-db
spec:
  selector:
    app: mi-db
  ports:
    - port: 5000
      targetPort: 5000
      name: http
  type: ClusterIP
